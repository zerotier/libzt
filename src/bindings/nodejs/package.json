{
  "name": "libzt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "mkdir -p libzt; cd libzt; ln -sf ../../../../include; ln -sf ../../../../dist",
    "build": "node-gyp configure build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-addon-api": "*",
    "node-gyp": "^9.3.1",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.2.5"
  }
}
